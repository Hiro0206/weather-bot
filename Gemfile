source 'https://rubygems.org'

gem 'sinatra'
gem 'sinatra-contrib'
gem 'rest-client'
gem 'line-bot-api'
gem 'rufo'
gem 'rake'

group :development do
  gem 'dotenv'
end

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
